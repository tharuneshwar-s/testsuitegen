import pytest
from constraints_numeric import validate_age

# Auto-generated by TestSuiteGen
# Operation: validate_age


class Testvalidateage:
    @pytest.mark.parametrize(
        "intent, kwargs, expected_status",
        [
            pytest.param(
                "PythonIntentType.HAPPY_PATH",
                {"age": 18},
                200,
                id="PythonIntentType.HAPPY_PATH",
            ),
            pytest.param(
                "PythonIntentType.BOUNDARY_MIN_MINUS_ONE",
                {"age": -1},
                422,
                id="PythonIntentType.BOUNDARY_MIN_MINUS_ONE",
            ),
            pytest.param(
                "PythonIntentType.NEGATIVE_VALUE",
                {"age": -1},
                422,
                id="PythonIntentType.NEGATIVE_VALUE",
            ),
            pytest.param(
                "PythonIntentType.BOUNDARY_MAX_PLUS_ONE",
                {"age": 151},
                422,
                id="PythonIntentType.BOUNDARY_MAX_PLUS_ONE",
            ),
        ],
    )
    def test_validate_age_contract(self, intent, kwargs, expected_status):
        """
        Contract testing for validate_age.
        Intent: {intent}
        Expected: {expected_status} (400/422 = Exception)
        """

        # Negative Tests (Expect Exceptions)
        if expected_status >= 400:
            # We expect TypeError for structural issues or ValueError for constraints
            with pytest.raises((ValueError, TypeError, AssertionError)):
                validate_age(**kwargs)

        # Happy Path (Expect Return Value)
        else:
            result = validate_age(**kwargs)
            assert result is not None
